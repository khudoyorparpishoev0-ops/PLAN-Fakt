/** Валюта учёта на стороне интерфейса — зеркало server/src/currency.ts.
 *
 *  Заказчик выбрал вариант «одна валюта, без пересчёта»: все суммы в сомони.
 *  Селекторы валюты и курса убраны из форм ввода операции и заявок, экран
 *  «Курсы валют» в настройках скрыт этим флагом (сам экран цел — он просто
 *  не показывается, пока валюта одна).
 *
 *  Обратно к мультивалютности: снять SINGLE_CURRENCY здесь и в
 *  server/src/currency.ts, вернуть селекторы в формы (см. историю git). */

export const BASE_CURRENCY = 'TJS';

export const SINGLE_CURRENCY = true;
