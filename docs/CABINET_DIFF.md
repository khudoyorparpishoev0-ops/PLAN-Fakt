# Сверка кабинета бухгалтера с прототипом «Кабинет Бухгалтера.dc.html»

Дата сверки: 30.07.2026 (ШАГ 0.1). Экраны кабинета изначально делались без
прототипа, по описанию из README/ТЗ; после получения файла прототипа вся
структура, раскладка, отступы, тексты кнопок и цвета статус-бейджей приведены
к эталону. Ниже — оставшиеся сознательные расхождения и спорные места.

## Исправлено по прототипу (для истории)

- Общий фильтр-ряд (периоды День…Год, чипы Проект/Статус/Валюта, поиск)
  и KPI-ряд (Отправлено / На рассмотрении / Одобрено / Отклонено) теперь
  на всех экранах кабинета — `src/cabinet/CabinetApp.tsx`.
- Палитра статус-бейджей кабинета заменена на палитру прототипа
  (синий #3E6B8A/#E7F0F6/#4E86AE и т.д.) — `src/data/cabinet.ts` (карта `CB`).
- Формы: широкие карточки с 2-колоночной сеткой, нумерованные подписи
  «1) Проект…», поля 44px/radius 10, кнопка 48px с иконкой-самолётиком.
- Вкладки «ЗАЯВКА НА ПОЕЗДКУ / РАСХОДЫ НА АВТО» — карточка с нижней
  полосой-индикатором вместо сегмент-контрола.
- «Список всего / История»: 4 селекта-фильтра по прототипу, компактные
  графики (пончик 60px + спарклайн) в шапке «ГЛАВНЫЙ РЕЕСТР ЗАЯВОК»,
  текстовые вкладки, единые колонки таблицы, кнопки действий 32×32.
  Цвета пончика — из прототипа: #22935B / #E5A400 / #D24A3D.
- «Проекты»: карточка «МОИ ПРОЕКТЫ» со счётчиком и подсказкой в шапке;
  статусы: «В работе» — зелёный, «Плановый» — синий, «Завершён» — серый
  (карта PB прототипа, отличается от админ-панели).
- Профиль в сайдбаре: «Фаридун · Операционный бухгалтер», иконки меню
  из прототипа, бейдж колокольчика «3».

## Сознательные расхождения (со ссылками)

1. **Данные-фикстуры богаче прототипа и согласованы с админ-панелью.**
   Прототип использует проекты «IT-Khona Web», «Офис», «Альфа»; в приложении —
   проекты из `src/data/admin.ts` («Насосная станция Вахдат», «ГЭС Помир-1»…),
   чтобы заявки кабинета ссылались на те же проекты, что и админ-панель
   (важно для сида БД на шаге 1). Файлы: `src/data/cabinet.ts` (PAY/TRIPS/CARS).
2. **KPI и проценты пончика считаются из данных**, а не зашиты («12», «32 400»,
   46/30/24% в прототипе — статичные примеры). Файлы:
   `src/cabinet/CabinetApp.tsx` (KPI), `src/cabinet/HistoryScreen.tsx:~253` (donutItems).
3. **Месяц в шапке — «Октябрь 2026»** (прототип кабинета: «Июль 2026», прототип
   админ-панели: «Октябрь 2026»). Выбран единый месяц системы, совпадающий с
   датами fixture-данных. Файл: `src/cabinet/CabinetApp.tsx` (шапка).
4. ~~Кнопка «← Панель руководителя» — демо-переключатель ролей.~~
   Выполнено на шаге 2: переключатель удалён, вместо него настоящие вход
   (экран логина, роль в JWT определяет интерфейс) и кнопка «Выйти» в шапке.
   Профиль в сайдбаре показывает имя и роль вошедшего пользователя.
5. **Валидация форм по ТЗ**: кнопка отправки неактивна (opacity .45), пока форма
   невалидна; подсказка «Для суммы от 100 TJS чек обязателен». В прототипе кнопка
   всегда активна (submit — no-op), подсказки нет. Оставлено по ТЗ (п. 4.1–4.2).
   Файлы: `src/cabinet/PayRequestsScreen.tsx`, `src/cabinet/CarRequestsScreen.tsx`.
6. **Корзина (удаление) — только у статуса «Черновик»** (ТЗ, раздел 5: «удаление —
   только Черновик»). В фикстурах прототипа значок корзины показан у одобренной
   заявки #101 — расценено как неточность демо-данных прототипа; бизнес-правило
   ТЗ приоритетнее. Файл: `src/cabinet/HistoryScreen.tsx` (условие
   `r.status === 'Черновик'` в колонке «Действия»).
7. **Карточка заявки (шторка с историей статусов)** открывается по 👁 и по ID —
   в прототипе кнопка «Просмотр» без действия; карточка с историей статусов
   требуется ТЗ (п. 4.3). Файл: `src/cabinet/HistoryScreen.tsx` (drawer).
8. **Переключатель «Компьютер / Телефон»** в шапке кабинета статичен — как и в
   прототипе (обработчиков нет); мобильной версии кабинета в дизайн-пакете нет.
9. **Attach-зоны работают** (клик прикрепляет/открепляет файл-заглушку) — в
   прототипе зоны статичны. Прикреплённое состояние отображается той же зоной
   с именем файла и ✕.
10. **Модалка «Сменить пароль»** по клику на профиль в сайдбаре кабинета —
    в прототипе отсутствует; добавлена по требованию заказчика (пароли из
    seed временные, пользователь должен мочь их сменить). До подключения
    API (шаг 3) — демо-поведение: валидация (новый пароль ≥ 8 символов,
    совпадение повтора) + тост. Файл: `src/cabinet/CabinetApp.tsx`
    (ChangePasswordModal). В админ-панели форма смены пароля уже есть на
    прототипном экране «Настройки → Мой профиль».

## Решения заказчика (30.07.2026)

- **Ставка компенсации км** вынесена в настройки — `src/data/settings.ts`
  (`SETTINGS.kmRate`), по умолчанию `0` = не задана. Пока ставка не задана,
  поездки показываются в километрах, а не в деньгах: KPI «Одобрено» — денежная
  сумма + «· N км» в подписи; пончик «Расходы по категориям» — в кольце только
  деньги, в легенде у поездок «N км»; «Проекты» — суммы TJS + «+ N км» второй
  строкой. При задании ставки (> 0) поездки автоматически учитываются в деньгах.
  На шаге 1+ ставка переезжает в таблицу настроек БД.
- **Месяц интерфейса** — «Октябрь 2026» (единый по системе) — принято.
- **Корзина в реестре** — только у статуса «Черновик», по ТЗ — принято.

## ШАГ 3 — интерфейс на данных API (30.07.2026)

Экраны кабинета и админ-панели читают данные из БД через API; фикстуры
`src/data/*.ts` остаются источником сида. Следствия и сознательные решения:

1. **KPI, пончик, «Статистика по месяцам», суммы проектов кабинета** —
   считаются из реального списка заявок (`GET /api/requests`), а не из
   статичных примеров прототипа.
2. **«Требует внимания»** в админ-панели: демо-элемент «заявка Р-2116»
   заменён настоящими заявками кабинета в статусах «Отправлено» /
   «На рассмотрении»; «Согласовать» / «Отклонить» вызывают
   `PATCH /api/requests/:id/status`. Три сюжетных элемента прототипа
   (просрочка, перерасход, налоги) оставлены как есть.
3. **Одобрение создаёт плановую операцию расхода** (ТЗ, п. 5 и 8):
   `is_plan = true`, оплата «Не подтверждена», внешний ключ `req:<номер>`
   (повторное одобрение невозможно — одобренная заявка неизменяема).
   В журнале операций такая строка помечена тегом «План» вместо
   «Доходы/Расходы»; в план-факте появляется строкой с планом = сумме
   заявки и статусом «План · заявка З-NNN».
4. **Журнал операций** — весь журнал БД (fact-операции план-факта тоже
   операции), а не 11 строк примера прототипа. Значок «есть комментарий»
   у суммы убран: в данных нет отдельного признака, примечание и так
   выводится второй строкой.
5. **Таблицы «План–факт по категориям» (Показатели) и отчёт «План-Факт»**
   строятся из данных: категории агрегируются по статьям, раскрытие «▸»
   показывает разбивку по проектам (в прототипе цифры и строки были зашиты).
6. **Ставка компенсации км** переехала в БД: `settings.km_rate` (дирам/км),
   `GET /api/settings` для всех ролей, `PATCH /api/settings` — только админ.
   По умолчанию 0 — как и раньше, показываются километры.
7. **Даты новых заявок и операций** — реальная дата создания (сервер),
   а не «29.10.2026» из прототипа. Заголовок «Октябрь 2026» остаётся
   статичным текстом шапки.
8. **Суммы проектов админ-панели** (экран «Проекты», inF/outF/inP/outP) —
   пока фикстуры: в БД нет операций, покрывающих «исторические» суммы
   примера (1 850 000 и т.д.). См. открытый вопрос ниже.

## ШАГИ 4–7 — вложения, сторно, журнал, проекты, администрирование (31.07.2026)

1. **Вложения по-настоящему**: дроп-зоны форм кабинета загружают файл в
   MinIO (JPG/PNG/PDF до 10 МБ) сразу при выборе; 📷/📄 в реестре и чипы в
   шторках открывают файл. Имена из seed — заглушки без файлов (подсказка
   «демо-имя, файла нет»).
2. **Сторнирование** (ТЗ, п. 5): кнопка в карточке строки «План · заявка …»
   экрана «Расходы». Обратная запись в журнале, статус «Сторнировано»
   в план-факте и в колонке «Директор» кабинета.
3. **Журнал операций**: панель фильтров теперь рабочая и серверная
   (тип, подтверждение, период — два поля дат вместо текстового
   «Укажите период», счёт/контрагент/статья/проект — селекты, сумма от/до,
   поиск). Пагинация «Показать ещё» по 50. Кнопка «⋯» прототипа заменена
   кнопками «+ Доход» и «+ Расход»; полосы «Сегодня нет операций / Вчера
   и ранее» убраны (реальные даты). Значок «есть комментарий» у суммы
   убран ещё на ШАГЕ 3.
4. **Формы «Новый доход/расход»** прототипа стали рабочими: статья и счёт —
   из справочников, валюта с курсом (курс подставляется из БД), без
   фактической суммы создаётся плановая операция. Поля «Способ оплаты» и
   «Ответственный» прототипа убраны (в модели операции их нет — вопрос);
   дроп-зона документа операции отложена (вложения пока только у заявок).
5. **Проекты админ-панели — из БД**: seed заводит «входящие остатки»
   (`base:*`-операции и планы, статья «Прочие доходы/расходы», дата —
   старт проекта) так, что суммы совпадают с примером прототипа, а дальше
   живут по реальным операциям. Это решение вопроса «стартовые суммы»
   вариантом «остатки на начало». Статусы «Плановый/В работе» — по факту
   платежей, «Завершён» — вручную (ТЗ, п. 8). В карточке проекта —
   сводка по статьям из `GET /api/projects/:id/summary`.
6. **Excel** (критерий приёмки): кнопки «Excel» на «План-Факт» и «Проектах»
   скачивают настоящие .xlsx (`/api/export/report.xlsx`, `projects.xlsx`).
   Кнопки «PDF» и «Печать» остаются макетами прототипа.
7. **Настройки**: «Пользователи» (создание с временным паролем, блокировка),
   «Общие настройки» (ставка км), «Курсы валют», «История действий»
   (аудит-лог), «Мой профиль» (реальная смена пароля) — рабочие; остальные
   разделы — заглушки «этап 2» по прототипу. Колонка «Телефон» прототипа
   в списке пользователей дополнена email (логином).

## Открытые вопросы (правило в ТЗ не найдено)

- **Название/подпись бухгалтера**: прототип кабинета — «Фаридун», список
  пользователей админ-панели — «Мунира Саидова (Бухгалтер)» и «Фаррух Назаров».
  Принят «Фаридун» по прототипу кабинета.
- **Учётная статья плановой операции из заявки** — в ТЗ не названа.
  Принято: оплата → «Прочие расходы», поездка → «Транспортные расходы»,
  авто → «Расходы на обслуживание автотранспорта»
  (`server/src/requests/requests.service.ts`, KIND_ARTICLE). Уточнить у
  заказчика, нужна ли отдельная статья/подстатьи.
- **Стартовые суммы проектов** («Проекты» админ-панели): в фикстурах —
  накопленные суммы за всё время проекта, операций под них в БД нет.
  Варианты: ввести «остатки на начало» отдельной операцией/полем или
  считать только по операциям БД (суммы изменятся). Ждёт решения заказчика.
- **Отчётный месяц**: интерфейс подписан «Октябрь 2026» (данные примера),
  новые заявки/операции датируются реальной датой. Переключатель периода
  пока не фильтрует данные — согласовать поведение при переходе на живые
  данные (текущий месяц вместо октября?).
