# Исходное состояние перед аудитом

Снято 07.08.2026, до единой правки кода.

## Репозиторий

| Что | Значение |
| --- | --- |
| Ветка | `claude/design-handoff-implementation-3okq4o` |
| Коммит | `8f4c89f` |
| Незакоммиченных изменений | нет |

## Среда

| Что | Значение |
| --- | --- |
| Node | v22.22.2 |
| npm | 10.9.7 |
| PostgreSQL | 16.13 (Ubuntu 16.13-0ubuntu0.24.04.1) |
| Docker | 29.3.1 — демон поднят, но образы `node:20-alpine` не скачиваются: сеть окружения запрещает `production.cloudfront.docker.com` (403 от прокси) |
| База | локальная `ithona` с демо-данными; **боевой сервер в аудите не участвовал** |

## Сборка (до правок)

```
frontend  tsc --noEmit   → без ошибок
frontend  vite build     → успех, 598 КБ (154 КБ gzip)
backend   tsc --noEmit   → без ошибок
backend   tsc -p         → успех
```

## Уязвимости зависимостей (`npm audit`, до правок)

**Фронтенд — 2 (1 высокая, 1 средняя), обе только для среды разработки:**

| Уровень | Пакет | Суть |
| --- | --- | --- |
| high | vite | обход пути в обработке `.map` оптимизированных зависимостей |
| moderate | esbuild | dev-сервер отвечает на запросы любого сайта |

В продакшене фронтенд — статика в nginx, dev-сервер не запускается.

**Бэкенд — 10 (2 высокие, 8 средних):**

| Уровень | Пакет | Суть |
| --- | --- | --- |
| high | multer | отказ в обслуживании из-за неполной очистки |
| high | @nestjs/platform-express | через зависимость |
| moderate | body-parser | обход ограничения размера при неверном `limit` |
| moderate | qs, express, uuid, exceljs, file-type, @nestjs/common, @nestjs/core | транзитивные |

Мажорные версии не поднимались (запрет из задания); фиксация — в разделе «Осталось».

## Регресс-проверки до аудита

| Проверка | Утверждений | Итог |
| --- | --- | --- |
| `check:expense` | 5 | пройдена |
| `check:planfact` | 17 | пройдена |
| `check:currency` | 18 | пройдена |
| `check:export` | 21 | пройдена |

Итого 61 утверждение — все зелёные. Это и была отправная точка: аудит искал
то, что эти проверки не покрывают.
